function encrypt() {
    var message = document.getElementById('encrypted').value.toLowerCase();
    var encrypted = frase.replace(/e/img, "enter");
    var encrypted = frase.replace(/o/img, "ober");
    var encrypted = frase.replace(/i/img, "imes");
    var encrypted = frase.replace(/a/img, "ai");
    var encrypted = frase.replace(/u/img, "ufat");

}